using Toybox.WatchUi;
using Toybox.System;

class SettingsMenuTwoDelegate extends WatchUi.MenuInputDelegate {

    function initialize() {
        MenuInputDelegate.initialize();
    }

    function onMenuItem(item) {
        switch (item) {
            case :SpeedArcData: {
                
            }
        }
    }
}
